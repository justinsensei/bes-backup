# Cron Job: Bes email dispatch

**Job ID:** 9e1227d21111
**Run Time:** 2026-06-10 08:28:05
**Schedule:** every 10m

## Prompt

[IMPORTANT: The following skill(s) were listed for this job but could not be found and were skipped: bes-email-dispatch. Start your response with a brief notice so the user is aware, e.g.: '⚠️ Skill(s) not found and skipped: bes-email-dispatch']

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Output
The following data was collected by a pre-run script. Use it as context for your analysis.

```
NEW_FORWARDED_EMAILS: 1
- id=19eb176e0f745038 from='Justin Goff <justin@signlab.co>' subject='Fwd: ASL Bloom - Sign Language is Featured as App of the Day on the App Store' snippet='Make a note about this, just a plain note in my vault inbox, thanks! ---------- Forwarded message ---------- From: Apple &lt;apple-noreply@a'
```

The poller (output above) has detected new emails Justin forwarded to goff.justin+bes@gmail.com. The script output lists one line per new message id.

For each id in the list:
  1. Load the bes-email-dispatch skill if not already loaded.
  2. Run scripts/load_context.py <MESSAGE_ID> via the venv python (path is in the skill).
  3. Follow the skill: parse instruction, decide intent, write to the Obsidian vault, verify the file landed.
  4. If is_real is false, skip and note why.

At the end, reply with one concise line per processed message (success or skip + reason). The reply is delivered to Telegram, so keep it tight — single line per email, no preamble.

If the script output above this prompt is empty or says "no new events," reply with literally nothing (empty response). Hermes cron will treat empty as silent.

## Response

⚠️ Skill(s) not found and skipped: bes-email-dispatch
✅ ASL Bloom - Sign Language is Featured as App of the Day on the App Store → filed to vault as 2026-06-10-asl-bloom-sign-language-is-featured-as-app-of-the-day-on-the-app-store.md
